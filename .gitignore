# Python
__pycache__/
*.py[cod]
*.pyo

# Pipeline output (generated at runtime — not committed)
source/data/
source/digests/

# Environment files (keys never committed)
.env
source/.env

# macOS
.DS_Store

# Internal session notes (not a deliverable)
SESSION_LOG.md

# Design briefs and landing page (separate repo / Vercel)
DESKTOP_APP_DESIGN_BRIEF.md
WEB_APP_DESIGN_BRIEF.md
landing/
